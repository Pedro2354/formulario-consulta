<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Sistema de Gestão de Consultas UVV</title>
    <link rel="stylesheet" href="~/css/site.css" />
</head>
<body>
    <header>
        <nav class="navbar">
            <a class="navbar-brand" asp-controller="Home" asp-action="Index">Consultas UVV</a>
            <div class="navbar-links">
                @if (User.Identity != null && User.Identity.IsAuthenticated)
                {
                    <span class="navbar-user">Olá, @User.Identity.Name</span>
                    <a asp-controller="Consultas" asp-action="Index">Minhas Consultas</a>
                    <form asp-controller="Account" asp-action="Logout" method="post" class="inline-form">
                        <button type="submit" class="link-button">Sair</button>
                    </form>
                }
                else
                {
                    <a asp-controller="Account" asp-action="Login">Entrar</a>
                    <a asp-controller="Account" asp-action="Register">Cadastrar</a>
                }
            </div>
        </nav>
    </header>

    <main class="container">
        @if (TempData["Mensagem"] != null)
        {
            <div class="alert alert-success">@TempData["Mensagem"]</div>
        }
        @RenderBody()
    </main>

    <footer class="footer">
        <p>&copy; @DateTime.Now.Year - Sistema de Gestão de Consultas UVV</p>
    </footer>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
